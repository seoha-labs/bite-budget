{
  "name": "bite-budget",
  "version": "0.1.0",
  "private": true,
  "description": "Game-inventory style meal expense tracker",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "emulate": "firebase emulators:start",
    "deploy": "firebase deploy"
  }
}
